// @flow

import React from 'react'
import styles from './App.css'
import Menu from './Menu'

export default () => (
	<div className={styles.app}>
		<Menu
			width={900}
			height={600}
		/>
	</div>
)
